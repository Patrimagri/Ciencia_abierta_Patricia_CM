pull
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("data/top10s.csv")
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("data/top10s.csv")
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("data/top10s.csv")
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data)
abline(lm(dur ~ year, data = data))
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data, main = "Tendencia de la duración de los hits",
xlab = "Año", ylab = "Duración (segundos)")
abline(lm(dur ~ year, data = data), col = "blue", lwd = 2)
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data, main = "Tendencia de la duración de los hits",
xlab = "Año", ylab = "Duración (segundos)")
abline(lm(dur ~ year, data = data), col = "blue", lwd = 2)
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data, main = "Tendencia de la duración de los hits")
abline(lm(dur ~ year, data = data))
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data)
abline(lm(dur ~ year, data = data))
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data, main = "Tendencia de la duración de los hits",)
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data, main = "Tendencia de la duración de los hits")
abline(lm(dur ~ year, data = data))
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data, main = "Tendencia de la duración de los hits")
abline(lm(dur ~ year, data = data))
---
title: "Ejercicio práctico Rmarkdown"
```{r setup, include = FALSE}
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("data/top10s.csv")
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data)
abline(lm(dur ~ year, data = data))
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data)
abline(lm(dur ~ year, data = data))
cat(mensaje, "Holis")
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data)
abline(lm(dur ~ year, data = data))
modelo <- lm(dur ~ year, data = data)
pendiente <- coef(modelo)[2]
if (pendiente > 0) {
mensaje <- "Las canciones se han vuelto más largas con el tiempo."
} else if (pendiente < 0) {
mensaje <- "Las canciones han reducido su duración con los años."
} else {
mensaje <- "No hay una tendencia clara en la duración de las canciones."
}
text(x = mean(data$year), y = mean(data$dur), labels = mensaje, col = "red", cex = 1.2)
cat(mensaje, "\n")
cat(mensaje, "\n")
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data)
abline(lm(dur ~ year, data = data))
modelo <- lm(dur ~ year, data = data)
pendiente <- coef(modelo)[2]
if (pendiente > 0) {
mensaje <- "Las canciones se han vuelto más largas con el tiempo."
} else if (pendiente < 0) {
mensaje <- "Las canciones han reducido su duración con los años."
} else {
mensaje <- "No hay una tendencia clara en la duración de las canciones."
}
cat(mensaje, "\n")
# Chunk 1: setup
knitr::opts_chunk$set(echo = FALSE)
# Cargar los paquetes necesarios
library(tidyr)
library(dplyr)
# Cargar los datos
data <- read.csv("top10s.csv")
# Chunk 2: top_artists
data %>% dplyr::count(artist) %>% arrange(desc(n)) %>% head()
# Chunk 3: top_genres
data %>% dplyr::count(top.genre) %>% arrange(desc(n)) %>% head()
# Chunk 4: duration_trend
plot(dur ~ year, data = data, main = "Tendencia de la duración de los hits",
xlab = "Año", ylab = "Duración (segundos)")
abline(lm(dur ~ year, data = data), col = "blue", lwd = 2)
modelo <- lm(dur ~ year, data = data)
pendiente <- coef(modelo)[2]
if (pendiente > 0) {
mensaje <- "Las canciones se han vuelto más largas con el tiempo."
} else if (pendiente < 0) {
mensaje <- "Las canciones han reducido su duración con los años."
} else {
mensaje <- "No hay una tendencia clara en la duración de las canciones."
}
cat(mensaje, "\n")
git status
